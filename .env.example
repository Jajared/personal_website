# Access key for the contact form, powered by Web3Forms (https://web3forms.com).
# It's free and needs only an email to verify — no account or dashboard required.
# Without this set, the form falls back to opening the visitor's mail client.
# Set it locally here (copy to .env) and in the Vercel project's environment variables.
VITE_WEB3FORMS_KEY=
